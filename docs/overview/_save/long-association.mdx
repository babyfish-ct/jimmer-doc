import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

## 1. DTO Code

```sh
export yourpackage.BookStore
    -> yourpackage.dto

input CompositeBookStoreInput {
    #allScalars
    #highlight-next-line
    books {
        #allScalars
        -id
    }
}
```

After compilation, Java/Kotlin type `CompositeBookStoreInput` will be automatically generated.

## 2. Code for saving

<Tabs groupId="language">
<TabItem value="java" label="Java">

```java
@PutMapping
public int saveBookStore(CompositeBookStoreInput input) {
    return sqlClient
        .save(input)
        .getTotalAffectedRowCount();
}
```

</TabItem>
<TabItem value="kotlin" label="Kotlin">

```kotlin
@PutMapping
fun saveBookStore(input: CompositeBookStoreInput): Int =
    sqlClient
        .save(input)
        .totalAffectedRowCount
```

</TabItem>
</Tabs>